  - name: update apt cache
    become: yes
    ansible.builtin.apt:
      update_cache: yes
      cache_valid_time: 30

  - name: updated installed packages
    become: yes
    ansible.builtin.package:
      upgrade: dist
