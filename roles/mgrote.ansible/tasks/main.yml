  - name: ansible installieren
    become: yes
    apt:
      name:
        - ansible
        - yamllint
      state: present

  - name: ansible-lint und yaml-lint installieren
    become: yes
    pip:
      name:
        - ansible-lint
      state: present
