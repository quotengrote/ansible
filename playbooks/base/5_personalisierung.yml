---
  - hosts: all
    roles:
      - { role: mgrote.motd, tags: "motd" }
      - { role: mgrote.tmux,
          tags: "tmux",
          when: "not 'virt' in group_names" }
      - { role: geerlingguy.dotfiles,
          ansible_user: "{{ dotfiles_user }}" ,
          tags: "dotfiles" }
