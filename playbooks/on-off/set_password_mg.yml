---
- hosts: all
  become: yes
  tasks:
    - name: Change user password
      user:
        name: mg
        update_password: always
        password: "{{{{ lookup('keepass', 'restic_repository_password', 'password') }}|password_hash('sha512') }}"
